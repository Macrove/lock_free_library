Phase 1: Lock-Free SPSC vs Locked SPSC
1. Implement the Baseline: Locked SPSC                                                                                                            DONE
  a. Write a simple SPSC queue using a std::mutex + std::condition_variable or spinlock.                                                          DONE
  b. Make sure both push() and pop() operations are blocking-free for correctness comparison.                                                     DONE
  c. Ensure that both implementations (lock-free and locked) have identical APIs.                                                                 DONE
2. Set Up Benchmarking Environment (Google Benchmark)
  Consistently and correctly measure the performance of each queue under a controlled test.
  To-Do:
    Create Google Benchmark tests for:
      push then pop                                                                                                                               DONE
      push+pop in a loop (i.e., real usage)                                                                                                       DONE
    Run benchmarks in two-threaded mode (1 producer, 1 consumer).                                                                                 DONE
    Pre-allocate memory where needed to avoid malloc variance.                                                                                    NOT_NEEDED
    Use large enough iterations (e.g., 1M ops per thread) to reduce noise.                                                                        NOT NEEDED (done automatically)

3. Pin Threads to CPU Cores
  To reduce OS scheduler noise.
  To-Do:
    Use OS-level APIs for core pinning:pthread_setaffinity_np
    Pin producer to one core, consumer to another adjacent core (on the same NUMA node ideally).
    Benchmark with and without pinning to observe impact.
    can wrap this into a utility function or RAII class for cleaner code.
    NOTE DOWN how you did the isolations and your other notes

4. Profile Hotspots (Micro-optimization Focus)
  Figure out which parts of your SPSC queue implementation are causing overhead.
  Tools:
    Linux: perf, valgrind --tool=callgrind, gprof, or Intel VTune (if available).
  To-Do:
    Profile under benchmark conditions (pinned threads, real throughput).
  Analyze:
    Branch mispredictions
    Cache misses
    Memory barriers (std::atomic vs relaxed atomics)
    False sharing (ensure consumer/producer don’t access same cache line)

5. Reduce Measurement Variance
  To get stable and repeatable results.
  To-Do:
    Disable turbo boost, power saving (e.g., via BIOS or cpupower frequency-set)
    Isolate CPU cores (e.g., with isolcpus= kernel flag)
    Run benchmarks on idle system (e.g., boot into a clean runlevel)
    Use taskset or numactl for further control

Phase 2: Statistical Comparison (U-Test, Confidence Intervals)
6. Extract Raw Benchmark Data
  To-Do:
  Use --benchmark_out=output.json with Google Benchmark
  Write a script (Python or R) to:
  Load timing data
  Calculate mean, median, stddev, variance
  Plot histograms / violin plots

7. Apply Statistical Tests (U-Test)
  The Mann–Whitney U-test is great when comparing two independent samples (non-parametric).
  Tools:
    Python: scipy.stats.mannwhitneyu
    R: wilcox.test()
  TODO:
  Collect ~30 runs of each benchmark (to get proper distributions)
  Perform U-test on:
    push() latency
    pop() latency
    combined throughput
    Report p-value and confidence level (e.g., 95% confidence that lock-free is faster)

Phase 3: Visualization & Reporting
8. Plotting and Diagrams
    To-Do:
      Plot latency histograms for each queue (lock-free vs locked)
      Line plots of throughput vs time
      CPU usage graphs (optional)
      Add diagrams of queue structure:
      One for locked SPSC
      One for lock-free SPSC (highlight cacheline layout, atomic ops, etc.)

Phase 4: Queue Library Foundation
9. Structure the Repo
  To-Do:
    Organize each queue type into separate modules:
      /include/lockfree/
          spsc.hpp
          mpsc.hpp
          ...
      /benchmark/
          bench_spsc_locked.cpp
          bench_spsc_lockfree.cpp
      /test/
          test_spsc.cpp


    Abstract common benchmarking utilities (e.g., thread pinning, test runners)

Phase 5: Iterate for Other Queues (MPSC, MPMC, etc.)

Once SPSC is rock solid and well-benchmarked:

Optional (Advanced Goals)
  Add memory reclamation strategies (Hazard pointers, epoch-based GC)
  Compare bounded vs unbounded queues
  Try using std::atomic_ref (C++20) or seqlock for low-contention scenarios
  Add false sharing tests (using padding, alignas(64))
