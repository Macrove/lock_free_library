Phase 1: Lock-Free SPSC vs Locked SPSC
1. Implement the Baseline: Locked SPSC                                                                                                            DONE
  a. Write a simple SPSC queue using a std::mutex + std::condition_variable or spinlock.                                                          DONE
  b. Make sure both push() and pop() operations are blocking-free for correctness comparison.                                                     DONE
  c. Ensure that both implementations (lock-free and locked) have identical APIs.                                                                 DONE
2. Set Up Benchmarking Environment (Google Benchmark)
  Consistently and correctly measure the performance of each queue under a controlled test.                                                       DONE
  To-Do:
    Create Google Benchmark tests for:
      push then pop                                                                                                                               DONE
      push+pop in a loop (i.e., real usage)                                                                                                       DONE
    Run benchmarks in two-threaded mode (1 producer, 1 consumer).                                                                                 DONE
    Pre-allocate memory where needed to avoid malloc variance.                                                                                    NOT_NEEDED
    Use large enough iterations (e.g., 1M ops per thread) to reduce noise.                                                                        NOT NEEDED (done automatically)

3. Pin Threads to CPU Cores
  To reduce OS scheduler noise.
  To-Do:
    // command to check isolated cpus - cat /sys/devices/system/cpu/isolated
    // command to check hyperthreading enabled : cat /sys/devices/system/cpu/smt/active
    // disable hyperthreading - reboot > grub's UEFI firmware settings > mark intel hyperthreading tech as disabled
    // how to isolate cpus
    //    cat /proc/interrupts - shows the IRQ spread
    //    vim /etc/default/irqbalance , add this line, IRQBALANCE_BANNED_CPUS=0xC. 0xC bans 2 and 3 from irq spread. if irqbalance not installed, then sudo apt install irqbalance. then ban
    //    vim /etc/default/grub
    //      modify "GRUB_CMDLINE_LINUX_DEFAULT="quiet splash"" to this "GRUB_CMDLINE_LINUX_DEFAULT="quiet splash isolcpus=2,3 nohz_full=2,3 rcu_nocbs=2,3""
    //          quite is for logging during boot, splash is also useless. can remove both, isolcpu is for u. nohz_full, no timer interrupts, rcu_nocbs - no rcu callbacks. read copy update callbacks. beto manage system procs
    // disable cpu scaling for google benchmark - sudo cpupower frequency-set --governor performance
    Use OS-level APIs for core pinning:pthread_setaffinity_np                                                                                       DONE
    Pin producer to one core, consumer to another adjacent core (on the same NUMA node ideally).                                                    DONE
    Benchmark with and without pinning to observe impact                                                                                            HOWTF. DONE
    NOTE DOWN how you did the isolations and your other notes                                                                                       DONE

4. Profile Hotspots (Micro-optimization Focus)
  Figure out which parts of your SPSC queue implementation are causing overhead.
  Tools:
    Linux: perf, valgrind --tool=callgrind, gprof, or Intel VTune (if available).
  To-Do:
    Profile under benchmark conditions (pinned threads, real throughput).
  Analyze:
    Branch mispredictions
    Cache misses
    Memory barriers (std::atomic vs relaxed atomics)
    False sharing (ensure consumer/producer don’t access same cache line)

5. Reduce Measurement Variance
  To get stable and repeatable results.
  To-Do:
    Disable turbo boost, power saving (e.g., via BIOS or cpupower frequency-set)                                                        DONE
    Isolate CPU cores (e.g., with isolcpus= kernel flag)                                                                                DONE
    Run benchmarks on idle system (e.g., boot into a clean runlevel)



NEXT PHASE: Iterate for Other Queues (MPSC, MPMC, etc.)

